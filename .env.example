# JWT Configuration
JWT_SECRET=your-secret-key-here-change-in-production

# CORS Configuration
# Use '*' for all origins (development only) or comma-separated list: 'http://localhost:3000,https://example.com'
CORS_ORIGIN=*

# Server Configuration
PORT=3000

# Logging
LOG_LEVEL=info

# Environment
NODE_ENV=development

# Default Admin User (temporary - will be replaced by database)
# Password will be hashed on server startup
DEFAULT_ADMIN_PASSWORD=admin

# Database Configuration
DATABASE_PATH=./data/plug_backend.db
